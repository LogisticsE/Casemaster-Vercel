# Required: Postgres connection string. Vercel reads this from the
# Project Settings → Environment Variables panel in production.
DATABASE_URL=postgresql://user:pass@host/db?sslmode=require

# Optional: where the .cms application lives, relative to the project
# root. Defaults to `app/`.
# CMS_APP_DIR=app
