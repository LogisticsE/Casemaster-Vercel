{
  "name": "cms-vercel",
  "version": "0.0.1",
  "private": true,
  "description": "Run CaseMaster .cms applications on Vercel",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc --noEmit",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/pg": "^8.11.0",
    "@vercel/node": "^3.2.0",
    "typescript": "^5.4.0",
    "vercel": "^39.0.0",
    "vitest": "^1.6.0"
  }
}
